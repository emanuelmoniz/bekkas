<x-guest-layout>
    <div class="text-center">
        <h1 class="mt-6 text-3xl font-extrabold text-dark">{{ $maintenance_title }}</h1>
        <p class="mt-2 text-lg font-medium text-primary">{{ $maintenance_subtitle }}</p>
        <p class="mt-3 text-grey-dark">{{ $maintenance_text }}</p>
    </div>
</x-guest-layout>
